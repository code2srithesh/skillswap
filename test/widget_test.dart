import 'package:flutter_test/flutter_test.dart';

import 'package:skillswap/main.dart';

void main() {
  test('SkillSwapApp can be constructed', () {
    const app = SkillSwapApp();
    expect(app, isNotNull);
  });
}
